# Rust build output
target/

# SQLite database files
*.db
*.db-shm
*.db-wal

# Environment variables
.env

# Personal rule configs — use rules.example.toml as your starting point
rules-live.toml
rules-test.toml
rules.toml

# Node.js / relay (removed — Rust posts directly to OpenClaw)
moltbook-relay/

# Runtime logs
alerts.log
*.log

# Personal Moltbook rule files (contain internal webhook URLs)
rules-moltbook.toml
rules-test-moltbook.toml
