# Base node WebSocket URL — must support the flashblocks feed
# Default uses the public Base flashblocks endpoint (no key needed)
BASE_WS_URL=wss://mainnet.flashblocks.base.org/ws

# Base node HTTP RPC URL — used for JSON-RPC calls (info, track)
# Can use the public endpoint or your own node / Alchemy / QuickNode URL
BASE_RPC_URL=https://mainnet.base.org

# Moltbook relay bind address and port
# Default: 127.0.0.1 (localhost only). Set to your Tailscale IP for VPN-only access.
RELAY_BIND=127.0.0.1
RELAY_PORT=4747
